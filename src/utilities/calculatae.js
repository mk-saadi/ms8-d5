const add = (first, second) => {
	return first + second;
};

const multiply = (first, second) => {
	return first * second;
};

//# ------------------------------- to export ------------------------------- */
// export default add
export { add, multiply };
